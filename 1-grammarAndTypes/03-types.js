// Boolean: has two possible values of either true or false

let on = true;
console.log(on);

let off = false;
console.log(off);

// Null: null is an empty value. Think of it as an empty container, Nothing is in it, but it can be filled in later.

let empty = null;
console.log(empty);

// Undefined: no value is assigned and does not act as any empty container

let undef = undefined;
console.log(undef);

let correct;
console.log(correct);

// Numbers: numbers are literally just numbers. also referred to as integers

let degrees = 85;
console.log(degrees);

let precise = 999999999999999;
console.log(precise);

let rounded = 99999999999999999
console.log(rounded);

let notQuite = 0.2 = 0.1;
console.log(notQuite);

let a = Number('123');
console.log(a);

// Strings: datatype used to represent text. Either wrapped in single or double quotes

let stringOne = 'singlequotes';
let stringTwo = "double quotes";
console.log(first);

let second = '1050' + '100';
console.log(second)


// objects: objects are used to store many values instead of a sigular value
// objects: hold key / value pairs

let frodo = {
    race:'hobbit'
    rings:'1'
    cloak: 'true'
}

console.log(frodo);
console.log(typeof frodo)

// Arrays: containers that hold a list of items

let burritos = ['large', 4 true];
console.log(burritos);
console.log(typeof burritos);

// addition and concatenation
let third = 1050 + '100';
console.log(third);

// String Methods: methods are tools that help us manipulate our data
// properties use '', methods use '.Methods()'

let myNameIs + 'Zach'
console.log(MyNameIs.toUpperCase())

let home = 'my home is greenwod';
console.log(home.includes('greenwood'));

let sent = 'this sentence will be split into individual parts';




